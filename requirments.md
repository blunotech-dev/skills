##format = name:description

trace-execution: Walk through the call stack step-by-step before answering.

isolate-variable: Identify the single changing variable causing the fault.

hypothesize-before-fix: State the suspected root cause before writing any code.

check-boundary-conditions: Evaluate how the logic handles extreme limits.

validate-inputs-first: Ensure the data entering the function is the expected shape.